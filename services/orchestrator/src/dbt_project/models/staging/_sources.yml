version: 2

sources:
  - name: forex
    schema: main
    tables:
      - name: oanda_ticks
        meta:
          external_location: "{{ var('bronze_path') }}/ticks/oanda/*/*.parquet"
      - name: oanda_m1
        meta:
          external_location: "{{ var('bronze_path') }}/ohlc_m1/*/*.parquet"
